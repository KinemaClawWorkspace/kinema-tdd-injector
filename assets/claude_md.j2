<!-- kinema-tdd-injector: injected -->
<!-- generated at: {{ iso_date }} | packages: {{ pkg_list }} -->

# 研发自动化测试规范 (CLAUDE.md)

> 适用对象：Claude Code 编码代理 + 前后端研发团队
> 由 `kinema-tdd-injector` skill 生成

---

## 1. 核心原则

本项目采用 **{% if test_integration %}三阶{% else %}两阶{% endif %}测试体系**，按运行环境隔离：

| 阶段       |{% if backend %} {{ backend_pkg }} 目录                            |{% endif %}{% if frontend %} {{ frontend_pkg }} 目录                             |{% endif %} 执行者          | 外部依赖          |
| -------- |{% if backend %} ------------------ |{% endif %}{% if frontend %} ------------------- |{% endif %} ------------ | ------------- |
| 单元测试     |{% if backend %} `tests/unit/{{ backend_pkg }}/`            |{% endif %}{% if frontend %} `tests/unit/{{ frontend_pkg }}/`            |{% endif %} Agent 自动 / 本地 | ❌ 无           |
| 开发环境集成测试 |{% if backend %} `tests/dev-integration/{{ backend_pkg }}/` |{% endif %}{% if frontend %} `tests/dev-integration/{{ frontend_pkg }}/` |{% endif %} Agent 自动 / 本地 | ❌ 无（Mock / MSW） |
{% if test_integration %}
| 测试环境集成测试 |{% if backend %} `tests/test-integration/{{ backend_pkg }}/` |{% endif %}{% if frontend %} —— (前端无此阶段)                       |{% endif %} **用户手动**     | ✅ 真实数据库       |
{% endif %}

{% if frontend %}
**前端仅有两阶**：`unit` + `dev-integration`。**不存在** `tests/test-integration/{{ frontend_pkg }}/`。前端的 e2e 验证由 Claude Code 通过 MCP 工具**手动驱动**（启动服务 → 操作 Playwright → 读页面 → 断言），不写自动化 e2e 代码。
{% endif %}

---

## 2. 测试路径三层命名规则

测试路径由三层组合而成，**逐层强制**：

| 层 | 规则 |
| - | - |
| **阶段层** | 测试根必为 `tests/unit/`、`tests/dev-integration/`{% if test_integration %}、`tests/test-integration/`{% endif %} |
| **包层** | 阶段根的下一级必为仓库顶层应用包名（本项目：{{ pkg_list_inline }}）。新增顶层应用包时**必须**在所有阶段根下同步建立同名目录 |
| **模块层** | 包名目录下的子目录与该包源码内部结构 **1:1 对齐**，不得压缩或省略中间目录 |

### 2.1 包源码根折叠

包内的源码根目录（{{ source_root_list }}）在测试树中 **折叠掉**：

```
{% if backend %}
{{ backend_pkg }}/{{ backend_src_root }}/service/task_service.py
  → tests/unit/{{ backend_pkg }}/service/test_task_service.py     ✅
  → tests/unit/{{ backend_pkg }}/{{ backend_src_root }}/service/test_task_service.py ❌
{% endif %}
{% if frontend %}
{{ frontend_pkg }}/{{ frontend_src_root }}/components/calendar/Cell.tsx
  → tests/unit/{{ frontend_pkg }}/components/calendar/Cell.test.tsx     ✅
  → tests/unit/{{ frontend_pkg }}/{{ frontend_src_root }}/components/calendar/Cell.test.tsx ❌
{% endif %}
```

### 2.2 文件命名规则

| 语言 | 源 | 测试 |
| - | - | - |
| **Python** | `<pkg-source-root>/<path>/<module>.py` | `tests/<阶段>/<pkg>/<path>/test_<module>.py` |
| **TS/TSX** | `<pkg-source-root>/<path>/<Name>.ts(x)` | `tests/<阶段>/<pkg>/<path>/<Name>.test.ts(x)` |

**强制约束**：
- Python 测试名 = `test_` + **完整**源文件名（包括 `_router` / `_service` / `_dao` 等后缀），不许缩写、不许用功能名替代
- TS 测试名 = **原始大小写**源文件名 + `.test`（PascalCase 组件保持 PascalCase，camelCase hook 保持 camelCase）
- Python 与 TS 命名规范不同，是为了贴合各自社区工具链（pytest 默认匹配 `test_*`、vitest 默认匹配 `*.test.*`）

### 2.3 归属判定原则

测试归到哪个子目录，**只看源文件物理位置**，与功能语义、模块名含义无关：

- `models/fts.py`（FTS 是搜索功能） → `models/`，**不归** `service/` 或 `search/`
- 包根 `main.py`（FastAPI 入口等） → 包根 `tests/<阶段>/<pkg>/test_main.py`
- `core/lifespan.py`（启动钩子） → `core/`

### 2.4 测试文件拆分规则（C+D 组合）

测试**原则上单文件**，用 `class`（Python）/ `describe`（TS）分组：

```python
{% if backend %}
# tests/unit/{{ backend_pkg }}/service/test_task_service.py
class TestCreate: ...
class TestQuery: ...
class TestDelete: ...
{% endif %}
```

若单测试文件超过 **800 行**仍难维护，**先考虑拆源码**（源文件已职责过载），源码拆完测试自动 1:1 跟随。**禁止单独拆测试文件而源码不动**。

### 2.5 测试数据文件

非测试代码的辅助数据（JSON / CSV / SQL dump / 图片）一律放 `tests/data/`，按所属测试路径再镜像：

```
{% if backend %}
tests/data/{{ backend_pkg }}/service/task_service/large_input.json
{% endif %}
{% if frontend %}
tests/data/{{ frontend_pkg }}/components/calendar/sample-events.json
{% endif %}
```

- `tests/data/` 不放任何 `*.py` / `*.ts` 测试代码
- 超大 fixture（GB 级 sample）走 LFS 或外部存储，**不进 git**

---

## 3. Agent 自动行为规范

### 3.1 完成以下任务后，必须自动运行测试

{% for ev in auto_trigger_events %}
- {{ ev }}
{% endfor %}

### 3.2 自动运行的命令序列

{% if backend %}
**后端**（工作目录：`{{ backend_pkg }}/`）：

```bash
cd {{ backend_pkg }}
{% for stage in auto_trigger_stages %}
uv run pytest ../tests/{{ stage }}/{{ backend_pkg }}/ -v
{% endfor %}
```
{% endif %}

{% if frontend %}
**前端**（工作目录：`{{ frontend_pkg }}/`）：

```bash
cd {{ frontend_pkg }}
{% for stage in auto_trigger_stages -%}
{% if stage == "unit" %}{{ frontend_pkg_manager }} test:unit
{% else %}{{ frontend_pkg_manager }} test:integration
{% endif %}
{%- endfor %}
```
{% endif %}

{% if backend %}
**激活规则**：一律使用 `uv run`（自动管理 venv），**禁止**在命令前添加 `source .venv/bin/activate` 或 `.venv\Scripts\Activate.ps1`。
{% endif %}

**准入规则**：自动触发的阶段必须 **100% 通过** + 覆盖率达标，否则不得提交代码。

{% if test_integration %}
### 3.3 测试环境集成测试（不自动运行）

代码提交并部署到测试服务器后，Agent 应**生成以下指令并提示用户手动执行**：

```
✅ 代码已推送。请在测试服务器上手动执行后端冒烟测试：

  cd {{ backend_pkg }} && uv run pytest ../tests/test-integration/{{ backend_pkg }}/ -v
{% if frontend %}

前端 e2e 由 Claude Code 通过 MCP Playwright 工具手动执行：
  1. 启动 {{ backend_pkg }} dev server
  2. 启动 {{ frontend_pkg }} dev server
  3. 用 Claude Code 驱动浏览器走关键路径
{% endif %}
```

Agent **不得**自行执行 `tests/test-integration/` 下的任何测试（本地无数据库连接权限，必然报错）。
{% endif %}

---

## 4. 网络边界规则

**原则**：**进程内（in-process）随便用，进程外（out-of-process）一律禁。**

**磁盘写入不计入"进程外 I/O"** —— 文件读写、本地 SQLite 文件、临时目录都属于进程内允许范围。

### 4.1 允禁对照表

| 行为 | unit | dev-integration |{% if test_integration %} test-integration |{% endif +%}
| - | - | - |{% if test_integration %} - |{% endif +%}
| `fastapi.TestClient` / `httpx.ASGITransport` | ✅ | ✅ |{% if test_integration %} ✅ |{% endif +%}
| `unittest.mock` / `pytest-mock` | ✅ | ✅ |{% if test_integration %} ✅ |{% endif +%}
| MSW（前端进程内拦截） | ✅ | ✅ |{% if test_integration %} — |{% endif +%}
| 文件读写 / 临时目录 | ✅ | ✅ |{% if test_integration %} ✅ |{% endif +%}
| SQLite `:memory:` | ✅ | ✅ |{% if test_integration %} ✅ |{% endif +%}
| SQLite 本地文件 | ✅ | ✅ |{% if test_integration %} ✅ |{% endif +%}
| 连真实数据库（MySQL/PG 等远程） | ❌ | ❌ |{% if test_integration %} ✅ |{% endif +%}
| 真实 HTTP 出栈（requests/urllib/未 mock httpx） | ❌ | ❌ |{% if test_integration %} ✅ |{% endif +%}
| DNS 解析非 localhost | ❌ | ❌ |{% if test_integration %} ✅ |{% endif +%}
| subprocess 启动网络服务 | ❌ | ❌ |{% if test_integration %} ✅ |{% endif +%}

---

## 5. 依赖声明

{% if dep_mode == "A" %}
### 5.1 声明源（唯一手改入口）

{% if backend %}
- **后端**：`{{ backend_pkg }}/pyproject.toml` 的 `[project.dependencies]` 与 `[project.optional-dependencies]`
{% endif %}
{% if frontend %}
- **前端**：`{{ frontend_pkg }}/package.json`
{% endif %}

### 5.2 锁定文件（命令生成，禁止手改）

| 文件 | 用途 |
| - | - |
{% if backend %}
| `{{ backend_pkg }}/uv.lock` | uv 完整解析锁 |
{% endif %}
{% if frontend %}
| `{{ frontend_pkg }}/{{ frontend_lockfile }}` | 前端锁 |
{% endif %}

**全部 lockfile 必须 commit 进 git。**

{% if backend %}
### 5.3 改后端依赖的流程

1. 编辑 `{{ backend_pkg }}/pyproject.toml`
2. 在 `{{ backend_pkg }}/` 下：
   ```bash
   uv lock
   ```
3. 将 `pyproject.toml` + `uv.lock` 一并 commit

**禁止**：手写 `requirements*.txt`、用 `pip freeze` 凑依赖、绕过 `uv lock`。
{% endif %}

### 5.4 环境初始化

```bash
{% if backend %}cd {{ backend_pkg }} && uv sync --extra test --extra dev
{% endif %}
{%- if frontend %}cd {{ frontend_pkg }} && {{ frontend_pkg_manager }} install
{% endif %}
```
{% elif dep_mode == "B" %}
### 5.1 声明源（唯一手改入口）

{% if backend %}
- **后端**：`{{ backend_pkg }}/pyproject.toml` 的 `[project.dependencies]` 与 `[project.optional-dependencies]`
{% endif %}
{% if frontend %}
- **前端**：`{{ frontend_pkg }}/package.json`
{% endif %}

### 5.2 派生锁定文件（命令生成，禁止手改）

| 文件 | 用途 |
| - | - |
{% if backend %}
| `{{ backend_pkg }}/requirements.txt` | 主依赖 pin 版本（pip 兼容兜底） |
| `{{ backend_pkg }}/requirements-test.txt` | test+dev 组 pin 版本 |
| `{{ backend_pkg }}/uv.lock` | uv 完整解析锁 |
{% endif %}
{% if frontend %}
| `{{ frontend_pkg }}/{{ frontend_lockfile }}` | 前端锁 |
{% endif %}

**全部 lockfile 必须 commit 进 git。**

{% if backend %}
### 5.3 改后端依赖的流程

1. 编辑 `{{ backend_pkg }}/pyproject.toml`
2. 在 `{{ backend_pkg }}/` 下重生派生文件：
   ```bash
   uv pip compile pyproject.toml -o requirements.txt
   uv pip compile pyproject.toml --extra test --extra dev -o requirements-test.txt
   uv lock
   ```
3. 将 `pyproject.toml` + 两份 `requirements*.txt` + `uv.lock` 一并 commit

**禁止**：跳过 step 2、单独手改 `requirements*.txt`、用 `pip freeze` 凑 requirements。
{% endif %}

### 5.4 环境初始化

```bash
{% if backend %}cd {{ backend_pkg }} && uv pip install -e ".[test,dev]"
{% endif %}
{%- if frontend %}cd {{ frontend_pkg }} && {{ frontend_pkg_manager }} install
{% endif %}
```
{% elif dep_mode == "C" %}
### 5.1 声明源（唯一手改入口）

{% if backend %}
- **后端**：`{{ backend_pkg }}/requirements.txt`（主依赖）+ `{{ backend_pkg }}/requirements-test.txt`（测试 + 开发依赖）
{% endif %}
{% if frontend %}
- **前端**：`{{ frontend_pkg }}/package.json`
{% endif %}

{% if backend %}
> 注：本项目采用旧式 `requirements.txt` 模式。**仍然强制使用 uv 作为 Python 运行时**（`uv pip install -r requirements.txt` / `uv run pytest`）。仅在依赖声明层面不引入 pyproject.toml。
{% endif %}

### 5.2 锁定文件

| 文件 | 用途 |
| - | - |
{% if backend %}
| `{{ backend_pkg }}/requirements.txt` | 主依赖（必须 pin 版本号） |
| `{{ backend_pkg }}/requirements-test.txt` | 测试 + 开发依赖（必须 pin 版本号） |
{% endif %}
{% if frontend %}
| `{{ frontend_pkg }}/{{ frontend_lockfile }}` | 前端锁 |
{% endif %}

{% if backend %}
**所有 requirements 文件必须 pin 到精确版本（`==X.Y.Z`），禁止使用 `>=` / `~=` 等模糊约束。**
{% endif %}

{% if backend %}
### 5.3 改后端依赖的流程

1. 编辑 `requirements.txt` 或 `requirements-test.txt`，pin 到精确版本
2. 在 `{{ backend_pkg }}/` 下重新同步环境：
   ```bash
   uv pip install -r requirements.txt -r requirements-test.txt
   ```
3. 跑测试验证，commit
{% endif %}

### 5.4 环境初始化

```bash
{% if backend %}cd {{ backend_pkg }} && uv venv && uv pip install -r requirements.txt -r requirements-test.txt
{% endif %}
{%- if frontend %}cd {{ frontend_pkg }} && {{ frontend_pkg_manager }} install
{% endif %}
```
{% endif %}

---

## 6. Conftest / Fixture 治理（方案 B）

### 6.1 Fixture 分层

| 文件 | 职责 |
| - | - |
| `tests/conftest.py` | **跨阶段通用数据 fixture**：`mock_<entity>` 系列（ORM-shape MagicMock，字段统一） |
{% if backend %}
| `tests/unit/{{ backend_pkg }}/conftest.py` | unit 特化：`mock_session`（基础 AsyncMock） |
| `tests/dev-integration/{{ backend_pkg }}/conftest.py` | dev-integration 特化：`mock_db_session`、`client_with_mock_db`（TestClient + dependency override） |
{% if test_integration %}
| `tests/test-integration/{{ backend_pkg }}/conftest.py` | test-integration 特化：`real_db_session` 等真库 fixture |
{% endif %}
{% endif %}

### 6.2 Fixture 命名约定

- **跨阶段同语义 fixture 必须同名**（如 `mock_task` 不再有 `mock_task_row` 双胞胎）
- 阶段特化的会话/客户端 fixture 可以分别命名（`mock_session` vs `mock_db_session` 反映了不同上下文的真实差异）

### 6.3 Fixture 定义位置

- **默认**：所有 fixture 定义在对应层的 `conftest.py`
- **例外**：仅当某 fixture **只服务一个测试文件**时，允许在该测试文件内 inline `@pytest.fixture`
- **禁止**：在测试文件内定义需跨文件共享的 fixture（应上提到 conftest）

---

## 7. 覆盖率门槛

### 7.1 阈值

| 范围 | 总覆盖率 | 每文件兜底 |
| - | - | - |
{% if backend %}
| 后端 | ≥ {{ backend_cov_total }}% | ≥ {{ backend_cov_per_file }}% |
{% endif %}
{% if frontend %}
| 前端 | ≥ {{ frontend_cov_total }}% | ≥ {{ frontend_cov_per_file }}% |
{% endif %}

### 7.2 计入测试阶段

- **计入**：`unit/` + `dev-integration/`
{% if test_integration %}
- **不计入**：`test-integration/`（本地跑不了，CI 也未必连得上真库）
{% endif %}

### 7.3 排除文件

{% if backend %}
- 后端：`{{ backend_src_root }}/__init__.py` 及各子包 `__init__.py`、未来的 alembic 迁移脚本
{% endif %}
{% if frontend %}
- 前端：`*.config.ts`、`next.config.ts`、`vitest.config.ts`
{% endif %}

{% if enforcement == "A" %}
### 7.4 强制点：本地 git hook（双触发）

| Hook | 触发 | 跑什么 |
| - | - | - |
| `pre-commit` | 每次 `git commit` | 快速通道：lint + 单元测试（≤ 10s） |
| `pre-push` | 每次 `git push` | 全套：单元 + 集成 + 覆盖率校验 |

**禁止**用 `git commit --no-verify` / `git push --no-verify` 绕过，除非已修复底层问题。

hook 脚本镜像位于 `scripts/git-hooks/`，由 `init_env` 软链到 `.git/hooks/`，否则新 clone 的同事不会自动获得 hook。
{% elif enforcement == "B" %}
### 7.4 强制点：CI

覆盖率与测试通过率由 CI 强制。本地不安装 git hook，但 PR 必须满足：

- 全部测试通过（单元 + dev-integration）
- 覆盖率达标（详见 §7.1）
- 未通过的 PR **禁止合并**，无论是否经过 review

**禁止**：在 CI 里用 `continue-on-error: true` 或类似机制让测试失败仍标记为绿。
{% elif enforcement == "C" %}
### 7.4 强制点：本地 git hook + CI 双保险

#### 本地 hook

| Hook | 触发 | 跑什么 |
| - | - | - |
| `pre-commit` | 每次 `git commit` | 快速通道：lint + 单元测试（≤ 10s） |
| `pre-push` | 每次 `git push` | 全套：单元 + 集成 + 覆盖率校验 |

hook 脚本镜像位于 `scripts/git-hooks/`，由 `init_env` 软链。**禁止**用 `--no-verify` 绕过。

#### CI

CI 是最终 gate。任一 job 失败 → PR 不可合并。**禁止** `continue-on-error: true`。

### 7.5 设计理由

本地 hook 给开发者快速反馈，CI 是不可绕过的底线。只有 hook 不够（容易被绕），只有 CI 不够（开发循环慢）。
{% elif enforcement == "D" %}
### 7.4 不设强制点

本项目目前**不通过自动化手段强制**覆盖率与测试通过。规则仍然有效——Agent 在完成任务后**必须主动跑测试**（详见 §3），但不靠 hook / CI 拦截。

**软约束**（违反 = 违规，只是不被自动拦截）：
- 在测试未通过的状态下 commit / push 业务代码
- 故意降低覆盖率以满足"工作完成"
- 用 `pytest.mark.skip` 掩盖失败而不在 PR 描述里说明原因
{% endif %}

---

## 8. 各阶段测试编写规范

### 8.1 单元测试（阶段一）

- **时机**：编码前或编码中（TDD 优先）
- **工具**：`pytest` + `unittest.mock` / `pytest-mock`；前端 `vitest` + `@testing-library/react`
- **禁止**：任何进程外 I/O（详见 §4）

{% if backend %}
按以下三层从底向上验证：

#### 8.1.1 模型 / 函数层

```python
# tests/unit/{{ backend_pkg }}/models/test_task.py
from {{ backend_src_root }}.models import Task

class TestTaskModel:
    def test_table_name(self):
        assert Task.__tablename__ == "tasks"
```

#### 8.1.2 服务层逻辑

```python
# tests/unit/{{ backend_pkg }}/service/test_task_service.py
from unittest.mock import MagicMock
from {{ backend_src_root }}.service.task_service import TaskService

class TestCreate:
    def test_create_returns_task(self):
        service = TaskService(dao=MagicMock())
        ...
```

#### 8.1.3 接口层契约（TestClient + 全 mock Service）

```python
# tests/unit/{{ backend_pkg }}/api/test_task_router.py
from unittest.mock import patch
from fastapi.testclient import TestClient
from {{ backend_src_root }}.main import app

client = TestClient(app)

@patch("{{ backend_src_root }}.service.task_service.TaskService.get_task")
def test_get_task_endpoint(mock_service):
    mock_service.return_value = {"id": "t001", "title": "X"}
    response = client.get("/tasks/t001")
    assert response.status_code == 200
```
{% endif %}

### 8.2 开发环境集成测试（阶段二）

- **时机**：模块开发完成、提交前
- **工具**：`pytest` + `unittest.mock.patch` 拦截 DAO 层；或 TestClient + dependency override
- **测什么**：Controller → Service → DAO 完整调用链路

{% if test_integration %}
### 8.3 测试环境集成测试（阶段三）

- **时机**：代码部署到测试服务器后
- **工具**：`pytest`，直连测试数据库（无 Mock）
- **测什么**：真实 SQL、ORM 映射、事务回滚、唯一索引约束
- **运行方式**：用户在测试服务器手动执行
{% endif %}

---

## 9. 快速参考

```bash
{% if backend %}
# 后端开发中（Agent 自动触发）
cd {{ backend_pkg }}
{% for stage in auto_trigger_stages %}
uv run pytest ../tests/{{ stage }}/{{ backend_pkg }}/ -v
{% endfor %}
{% endif %}

{% if frontend %}
# 前端开发中
cd {{ frontend_pkg }}
{% for stage in auto_trigger_stages -%}
{% if stage == "unit" %}{{ frontend_pkg_manager }} test:unit
{% else %}{{ frontend_pkg_manager }} test:integration
{% endif %}
{%- endfor %}
{% endif %}

{% if test_integration %}
# 部署后冒烟（用户手动，在测试服务器）
cd {{ backend_pkg }} && uv run pytest ../tests/test-integration/{{ backend_pkg }}/ -v
{% endif %}
```

---

## 10. Commit Message 规范

{% if commit_style == "A" %}
### 10.1 标题格式

格式：`<type>(<scope>): <subject>`

**type**：`feat`（新功能）/ `fix`（修补 bug）/ `docs`（文档）/ `style`（格式）/ `refactor`（重构）/ `test`（测试）/ `chore`（构建/工具）

**scope**：模块级（如 `api`、`dao`、`service`、`models`、`schemas`、`core`、`utils`）或跨模块（用逗号，如 `api, service`）

**subject**：
- 使用**中文**
- 结尾不加句号
- 简洁明确

### 10.2 正文

- 与标题空一行
- **中文**详述，列表用 `-` 或 `*`
- 复杂提交分小标题：主要改进 / 影响范围 / 变更细节

{% if ai_signature %}
### 10.3 脚注

AI 辅助完成需署名：

```
Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
{% endif %}
{% elif commit_style == "B" %}
### 10.1 Title format

Pattern: `<type>(<scope>): <subject>`

**type**: `feat` / `fix` / `docs` / `style` / `refactor` / `test` / `chore`

**scope**: module name (e.g. `api`, `dao`, `service`, `models`, `schemas`, `core`, `utils`) or cross-module (comma-separated)

**subject**:
- Write in **English**
- No trailing period
- Concise and specific

### 10.2 Body

- Blank line between title and body
- Write in **English**, bullet with `-` or `*`
- For non-trivial commits, group bullets under sub-headings: Highlights / Impact / Details

{% if ai_signature %}
### 10.3 Footer

AI-assisted commits must include attribution:

```
Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
{% endif %}
{% elif commit_style == "C" %}
### 10.1 自由格式

本项目对 commit message 不做硬约束。建议但不强制：

- 标题一行，简洁说明做了什么（中英文均可，结尾不加句号）
- 复杂改动空一行后写正文，解释**为什么**而不是**做了什么**
- 不必使用 `feat:` / `fix:` 前缀

### 10.2 仅有的硬约束

- **原子提交**：一个 commit 只做一件事
- **不许提交未通过测试的代码**
- **敏感信息**：永远不要 commit 密钥、token、`.env`

{% if ai_signature %}
### 10.3 脚注

AI 辅助完成的 commit 需要添加署名：

```
Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
{% endif %}
{% endif %}

{% if frontend %}
---

## 11. 前端包管理器

前端（`{{ frontend_pkg }}/`）统一使用 **{{ frontend_pkg_manager }}**，禁止使用其他包管理器。
{% endif %}
